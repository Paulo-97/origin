/*pogramme de calcul d'aire*/

#include <stdio.h>
#include <math.h>

main()
{
    float pi=3.14159;
    float rayon, surface;
    scaf ("%f", &rayon);
    surface:=pi*pow(raion*raion);
    printf("%f\", surface);
    return 0;
    }
